<view class="cates">
    <view class="container" >
    <navigator
     wx:for="{{leftMenuList}}"
     wx:for-item="item"
     wx:for-index="index"
     wx:key="id"
     url="/pages/categoryBook/index?categoryId={{item.id}}" open-type="navigate" >
     <image 
     src="{{item.categoryImgUrl}}" mode="scaleToFill">
     </image>
    </navigator>
    </view>
</view>