<view class="cates">
    <Searchinput></Searchinput> 
    <view class="container" >
    <navigator
     wx:for="{{leftMenuList}}"
     wx:for-item="item"
     wx:for-index="index"
     wx:key="id"
     url="/pages/categoryBook/index?categoryId={{item.id}}" open-type="navigate" >
     <image 
     src="{{item.homeImgUrl}}" mode="{{index===0?'widthFix':'scaleToFill'}}" >
     </image>
     <text class="text">
     {{item.name}}
     </text>
    </navigator>
    </view>
    <!-- <navigator url="/pages/categoryBook/index?categoryId={{item.id}}" open-type="navigate" >
        <view 
           class="menu_item"
           wx:for="{{leftMenuList}}"
           wx:key="this"
           data-index="{{index}}"
           >
           {{item}}
        </view> 
    </navigator>  -->
</view>