<view class="cates">
    <Searchinput></Searchinput> 
    <view class="container" >
    <navigator
     wx:for="{{leftMenuList}}"
     wx:for-item="item"
     wx:for-index="index"
     wx:key="id"
     url="/pages/categoryBook/index?categoryId={{item.id}}" open-type="navigate" >
     <image 
     src="{{item.homeImgUrl}}" mode="scaleToFill">
     </image>
     <view class="text">
     {{item.name}}
     </view>
    </navigator>
    </view>
    <!-- <navigator url="/pages/categoryBook/index?categoryId={{item.id}}" open-type="navigate" >
        <view 
           class="menu_item"
           wx:for="{{leftMenuList}}"
           wx:key="this"
           data-index="{{index}}"
           >
           {{item}}
        </view> 
    </navigator>  -->
</view>