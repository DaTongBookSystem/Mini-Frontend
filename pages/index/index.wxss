.index_zhiding {
  padding: 10rpx 10rpx;
  display: flex;
}
.index_zhiding image {
  border-radius: 15rpx;
  align-items: center;
  justify-content: center;
  height: 100%;
  width: 100%;
}
.index_swiper swiper {
  width: 750rpx;
  height: 340rpx;
  padding: 10rpx 10rpx;
}
.index_swiper swiper image {
  width: 100%;
}
.index_cate {
  display: flex;
}
.index_cate navigator {
  padding: 10rpx 10rpx;
  flex: 1;
}
.index_cate navigator text {
  justify-content: center;
  display: flex;
  color: var(--themeColor);
}
.index_cate navigator image {
  width: 100%;
}
.index_floor .floor_group .floor_title {
  padding: 10rpx 10rpx;
}
.index_floor .floor_group .floor_title image {
  width: 100%;
}
.index_floor .floor_group .floor_list {
  padding: 10rpx 10rpx;
}
.index_floor .floor_group .floor_list navigator {
  overflow: hidden;
  float: left;
  width: 50%;
  /* 后五个超链接 */
  /* 1 2 3 4 四个超链接 */
  /* 2 4 6 三个超链接 */
}
.index_floor .floor_group .floor_list navigator:nth-last-child(-n+5) {
  /* 原图的宽高 232 *386 */
  height: 450rpx ;
}
.index_floor .floor_group .floor_list navigator:nth-child(1) {
  height: 450rpx ;
}
.index_floor .floor_group .floor_list navigator:nth-child(1),
.index_floor .floor_group .floor_list navigator:nth-child(2),
.index_floor .floor_group .floor_list navigator:nth-child(3),
.index_floor .floor_group .floor_list navigator:nth-child(4) {
  border-bottom: 10rpx solid #fff;
}
.index_floor .floor_group .floor_list navigator:nth-child(2),
.index_floor .floor_group .floor_list navigator:nth-child(4),
.index_floor .floor_group .floor_list navigator:nth-child(6) {
  border-left: 10rpx solid #fff;
}
.index_floor .floor_group .floor_list navigator image {
  width: 100%;
}
